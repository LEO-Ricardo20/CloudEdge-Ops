{
  "name": "cloudedge-ops",
  "version": "0.2.0",
  "private": true,
  "description": "Persistent cloud-edge device operations MVP with telemetry, alerting, OTA and realtime observability",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "simulate": "node simulator/device-simulator.js",
    "test": "node --test tests/*.test.js",
    "test:integration": "node --test tests/http.test.js tests/persistence.test.js tests/simulator-smoke.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
